export enum TimeVal {
    HoursInDay = 24,
}

export enum QsParam {
    DateCode = 'dateCode',
}

export enum DialogResult {
    Ok = 'ok',
    Canceled = 'canceled',
    Saved = 'saved',
    Deleted = 'deleted',
}
